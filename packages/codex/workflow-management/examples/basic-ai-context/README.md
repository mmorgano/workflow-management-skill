# Basic AI Context Example

Copy this folder to a writable location, then set that location as
`ai_context_root` with `setup-skills.sh` or, on Windows, `setup-skills.ps1`. It
shows the smallest useful starting point for a session-driven project.

`LAST_SESSION.md` is intentionally absent. The workflow creates it only when
the first session is closed.
