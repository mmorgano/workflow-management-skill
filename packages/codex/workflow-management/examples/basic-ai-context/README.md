# Basic AI Context Example

Copy this folder to a writable location, then set that location as
`ai_context_root` with `setup-skills.sh`. It shows the smallest useful starting
point for a session-driven project.
